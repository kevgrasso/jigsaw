GIRL POWERED!! BY KEVIN GRASSO
A Javascript platformer for HTML5 Canvas.

Requires:
<canvas>
DOM events
DOMContentLoaded
script load event
native JSON
mouse e.pagex+e.pagey
localStorage
Array.isArray()
@font-face
<audio>

have backup:
web audio
clipboard
document.head
WOFF fonts


Recomended browsers:
Google Chrome 6+
Mozilla Firefox 3.5+
Safari 3.2+
Opera 10.50+
Internet Explorer 9+

~CHANGELOG~
proto2:
*using Iuppiter and Sylvester libraries
*basic functionality achieved: triggers, input, layers, and collision
*mass cleanup+bug fixes

proto1:
*project exists
*basic engine under construction
*basic player physics
*using pixastic library

~ROADMAP~
proto3:
*conversion to coffeescript *in progress*
*layers have contexts
*mass unsubscription for 
*game loop is entirely triggers
*change binary heaps to sorted array. option for sorting every frame + manual adjust order (sets adjustFrame = true)
*use linked list for tile-shape subscription
*shapes rewritten + using new collision formula

proto4:
*MetaLayer.getAbsPosOf(Vector)
*collision.getPointData()
*mouse input

upcoming proto:
*map display
*map movement
*map load
*map events
*map dynamic block definitions
*gamestate saving + loading
*
